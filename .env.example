# 后端基地址（Cloudflare Worker）。留空 = 纯前端本地模式（免费试用演示，无后端校验）。
# 部署并配置好 worker/backend.js 后，填入你的 Worker 地址即可启用前后端分离：
# VITE_API_BASE=https://ktb-backend.<your-subdomain>.workers.dev
VITE_API_BASE=

# 管理员登录口令（可选，覆盖默认的演示口令 wiz1/wiz2/wiz3）。
# 多个用逗号分隔；配置后重新 `npm run build` 即生效，源码中不会出现明文。
# 例：VITE_ADMIN_PASS=你的强口令1,你的强口令2
VITE_ADMIN_PASS=
